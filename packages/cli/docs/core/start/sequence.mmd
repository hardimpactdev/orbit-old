sequenceDiagram
    participant User
    participant CLI as Start
    participant Caddy as CaddyfileGenerator
    participant PHP as PhpManager
    participant CaddyMgr as CaddyManager
    participant Service as ServiceManager

    User->>CLI: orbit start
    CLI->>CLI: detect architecture
    CLI->>Caddy: generate config

    alt PHP-FPM architecture
        CLI->>PHP: start pools 8.3 and 8.4
        CLI->>CaddyMgr: start
    end

    CLI->>Service: startAll Docker services

    CLI-->>User: results with service status
